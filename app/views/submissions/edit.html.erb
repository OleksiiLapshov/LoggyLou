<% content_for :title, "Editing submission" %>

<div class="form-card">
  <h1 class="h1-style">Reject submission</h1>

  <%= render "form", submission: @submission %>

  <%= link_to "Show this submission", @submission, class: "button" %>
  <%= link_to "Back to submissions", submissions_path, class: "button-secondary" %>
</div>
