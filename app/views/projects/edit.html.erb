<% content_for :title, "Editing project" %>

<div class="form-card">
  <h1  class="h1-style">Editing project</h1>
  <%= render "form", project: @project %>
  <div class="mt-4">
    <%= link_to "Show this project", @project, class: "button" %> |
    <%= link_to "Back to projects", projects_path, class: "button-secondary" %>
  </div>
</div>
