<% content_for :title, "New project" %>

<div class="form-card">
  <h1  class="h1-style">New project</h1>
  <%= render "form", project: @project %>

  <div class="mt-4">
    <%= back_button %>
  </div>
</div>
